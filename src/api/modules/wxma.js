import { handleOptions, handleReturn } from "../conf";
// URL配置
const urlConf = {
  // 小程序登录，参数code
  login: {
    url: "/anon/wxma/login",
    method: "post",
  },
};

export default urlConf;
